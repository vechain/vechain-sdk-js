{
  "name": "sdk-user",
  "description": "An example app of how to integrate the VeChain SDK with Node.js",
  "version": "1.0.22",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "license": "MIT",
  "author": "VeChain Foundation",
  "scripts": {
    "build": "rm -rf ./dist && tsup src/index.ts --format cjs,esm --dts",
    "start": "yarn build && node dist/index.js",
    "test": "jest"
  },
  "dependencies": {
    "@vechain/sdk-network": "1.0.22",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "tsup": "^8.0.2"
  }
}
