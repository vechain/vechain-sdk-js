{
  "name": "sdk-user",
  "version": "1.0.0-beta",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ./dist && tsup src/index.ts --format cjs,esm --dts"
  },
  "dependencies": {
    "@vechain/vechain-sdk-core": "*",
    "typescript": "^5.3.3"
  }
}