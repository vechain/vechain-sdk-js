import { describe, expect, test } from '@jest/globals';

/**
 * Abstract provider tests
 *
 * @group integration/providers/abstract-provider
 */
describe('Abstract provider tests', () => {
    test('Simple test', () => {
        expect(true).toBe(true);
    });
});
