{
  "name": "@vechain/sdk-solo-setup",
  "version": "3.0.0-beta.1",
  "description": "VeChain Thor solo blockchain development environment CLI",
  "private": true,
  "license": "MIT",
  "bin": {
    "solo-setup": "./bin/cli.js"
  },
  "main": "dist/index.cjs",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.cjs",
      "import": "./dist/index.mjs"
    }
  },
  "files": [
    "dist",
    "types",
    "bin",
    "docker-compose.solo.yml",
    "hardhat.config.ts",
    "contracts",
    "cache",
    "artifacts",
    "config.json"
  ],
  "keywords": [
    "vechain",
    "thor",
    "blockchain",
    "development",
    "cli",
    "solo"
  ],
  "scripts": {
    "build": "rm -rf ./dist && npx tsdown index.ts scripts/deploy.ts --format cjs,esm --dts --out-dir dist",
    "clean": "rm -rf dist node_modules && rm -rf ./dist",
    "dev": "npm run build -- --watch",
    "compile": "npx hardhat compile",
    "deploy-solo": "npx hardhat run dist/scripts/deploy.cjs --network vechain_solo",
    "solo-up": "make solo-up",
    "solo-down": "make solo-down",
    "solo-seed": "yarn build && yarn deploy-solo"
  },
  "dependencies": {
    "@vechain/sdk-hardhat-plugin": "2.0.5",
    "@vechain/sdk-network": "2.0.5",
    "hardhat": "2.26.3"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "3.1.0",
    "@nomicfoundation/hardhat-toolbox": "4.0.0",
    "@typechain/ethers-v6": "0.5.1",
    "@typechain/hardhat": "9.1.0",
    "@types/mocha": "10.0.10",
    "ts-node": "10.9.2",
    "tsdown": "0.18.4",
    "typechain": "8.3.2",
    "typescript": "5.9.2"
  }
}
