{
  "name": "docs",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "rundocs": "npx runme run --all --category ci --skip-prompts",
    "test:examples": "ts-node-test examples/",
    "build:docs": "rm build/*.md && python build_docs.py"
  },
  "dependencies": {
    "@vechain-sdk/core": "*"
  },
  "devDependencies": {
    "expect": "^29.7.0",
    "runme": "^3.0.2",
    "thor-devkit": "^2.0.9",
    "ts-node": "^10.9.1",
    "ts-node-test": "^0.4.3"
  }
}
