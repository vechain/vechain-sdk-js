name: PR Checks

on:
  pull_request:
    branches:
      - 'main'
      - 'main-*'
      - 'supported-*'
      - 'feature-*'
      - 'sdk-*'

jobs:
    build-lint:
        uses: ./.github/workflows/build-lint.yml
        secrets: inherit


