{
  "name": "thor-sdk-js",
  "version": "1.0.0",
  "description": "The official JavaScript SDK for vechain",
  "main": "index.js",
  "repository": "https://github.com/vechainfoundation/thor-sdk-js.git",
  "author": "vechain Foundation",
  "license": "MIT",
  "scripts": {
    "postinstall": "husky install",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "^17.7.1",
    "@commitlint/config-conventional": "^17.7.0",
    "husky": "^8.0.0",
    "turbo": "^1.10.14"
  },
  "scripts": {
    "postinstall": "husky install",
    "test": "yarn workspaces run test",
    "lint": "yarn workspaces run lint"
  }
}